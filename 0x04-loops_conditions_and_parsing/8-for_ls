#!/usr/bin/env bash
# This script lists the files in the current directory, showing only the part after the first dash

for file in *; do
    # Skip directories or hidden files starting with .
    if [[ -f "$file" && "$file" != .* ]]; then
        # Extract the part after the first dash, or the whole name if no dash
        echo "${file#*-}"
    fi
done
